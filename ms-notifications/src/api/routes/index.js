// src/api/routes/index.js
const express = require('express');
const router = express.Router();

// Este microservicio no expone rutas de API más allá de las de salud.
// Su función es consumir eventos de forma asíncrona.

module.exports = router;